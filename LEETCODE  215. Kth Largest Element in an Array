#include<stdio.h>
#include<stdlib.h>
int findKthLargest(int* nums, int numsSize, int k) {
    int compare(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}
int newarray[numsSize];
int i=0;
for(i=0;i<numsSize;i++)
{
    newarray[i]=nums[i];
}
qsort(newarray,numsSize,sizeof(int),compare);
return newarray[numsSize-k];
}
